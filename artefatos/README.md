# artefatos

Criei a pasta `artefatos` e coloquei referências/copias (placeholders) para os projetos solicitados.

ATENÇÃO: por limitações do agente, não consigo remover arquivos ou pastas originais do repositório (incluindo o zip). Portanto esta operação copia/replica a estrutura solicitada em `artefatos/` e mantém os itens originais na raiz.

Itens referenciados:
- api-server -> ./api-server
- miar-aria -> ./miar-aria
- mockup-sandbox -> ./mockup-sandbox
- Zip detectado: Miar-Aria-final.zip
